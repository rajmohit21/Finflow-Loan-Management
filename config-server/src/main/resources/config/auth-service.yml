server:
  port: 8081
  forward-headers-strategy: framework

springdoc:
  api-docs:
    path: /auth/v3/api-docs
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/auth_db?createDatabaseIfNotExist=true
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SMTP_USERNAME:mohitraj2180@gmail.com}
    password: umvljhydryhynpqu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/
jwt:
  secret: aVeryLongSecretKeyForJwtValidationWhichMustBeAtLeast256BitsLong
  expiration: 86400000
