server:
  port: 8084
  forward-headers-strategy: framework

springdoc:
  api-docs:
    path: /admin/v3/api-docs
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/admin_db?createDatabaseIfNotExist=true
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: 5672
    username: admin
    password: admin
  mail:
    host: smtp.gmail.com
    port: 587
    username: finflow.demo@gmail.com
    password: fake-password-for-demo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/
